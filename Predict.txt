title Predict
participant Client
participant Frontend
participantgroup #lightblue Server Model
participant Server API (Flask)
end
participantgroup #lightgreen Database
participant Mongo
end
participantgroup #yellow Datastore
participant Amazon S3
end

Frontend->(2)Frontend:Preload some Models
box over Client, Amazon S3: Server Initialize
Client->Frontend: Input: Select Model, Upload Data
rbox right of Frontend: If Model is preloaded
Frontend ->Client : Computing on Client (Tensorflow JS)
rbox left of Frontend: If Model is not preloaded
Frontend->Server API (Flask): Fetch Model
Server API (Flask) -> Amazon S3: Get Model
Amazon S3 -->Server API (Flask): Model
Server API (Flask) ->Mongo : Save Logs
Server API (Flask) ->Frontend: Model
Frontend ->(2)Frontend:Update Model's Store (Vuex)
Frontend ->Client: Computing on Client (Tensorflow JS)
